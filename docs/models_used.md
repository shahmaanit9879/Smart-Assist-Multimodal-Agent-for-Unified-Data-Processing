# Advanced Models (Easy Explanation)

The project can use advanced multimodal models like:

- CLIP (for image + text matching)
- Flamingo (for mixing multiple types of data)

These models make the results more rich and detailed.