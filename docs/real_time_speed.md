# Real-Time Speed Optimization

To make the system faster:
1. Small models are used when possible.
2. Heavy tasks run after light tasks.
3. Agents process things in parallel if needed.

This reduces delay in multimodal inputs.